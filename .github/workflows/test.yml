name: 🧪

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Failing task
        run: |
          sh -c "echo subshell; false"
                    
          echo $?
          exit 0
