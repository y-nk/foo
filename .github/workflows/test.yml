name: 🧪

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Failing task
        run: |
          externalcommand() { exit 1 }
        
          f() {
            externalcommand || true
            echo $?
            externalcommand
            echo $?
            exit 0
          }
          
          f
