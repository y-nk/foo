name: 🧪

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Failing task
        run: |
           set +e
           set +o pipefail
           
           curl > output.log 2>&1
           cat output.log
