name: 🧪

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Failing task
        run: |
          bash -c "echo subshell; false; echo $?; exit 0"
                    
          echo "main exit code: $?"
          exit 0
