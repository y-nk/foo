name: 🧪

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Failing task
        run: |
          (
            echo 'subshell'
            false
          )
          
          echo $?
          exit 0
