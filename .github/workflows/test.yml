name: 🧪

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Failing task
        run: |
          set +e
          
          echo 'test'
          externalcommand() {
            echo 'in externalcommand'
            exit 1;
          };
        
          f() {
            echo 'calling externalcommand or true'
            externalcommand || true;
            echo $?;
            echo 'calling externalcommand'
            externalcommand;
            echo $?;
            echo 'calling exit0'
            exit 0;
          };
          
          f;
